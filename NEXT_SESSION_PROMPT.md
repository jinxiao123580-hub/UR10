# 下一个会话的提示词（DSH）

> 用途：开新对话时把下面代码块整段粘贴。**刻意写短** —— 提示词本身也占上下文，
> 细节都在仓库文件里，让新会话按需去读。

```text
继续 UR10 机械臂工程实践课程，本轮做第 1–6 节（第 7、8 节已决定延后，不采购硬件）。

第一步先读这三个文件（不要假设旧文档里的"已完成"仍然有效）：
1. ~/UR10/HANDOVER.md 顶部「0A. 最新接手状态」—— 唯一权威状态
2. ~/UR10/experiments/2026-09-18-八节课实施计划草案.md —— 逐节做法与前置依赖
3. ~/UR10/docs/00-当前可用操作.md —— 当前可执行入口

当前进度：第 1、2 节已完成并真机只读验证；第 3 节入口已有但只跑过 dry-run；
第 4、5、6 节未做，其中第 5 节被两个 P0 阻塞。

先做 HANDOVER 里的 P0-A（力标定的 2 倍质量矛盾：所有拟合收敛到 3.2 kg，
但单姿态 |F|/g 反推是 6.56 kg，相差 2.03 倍且极度可重复）。
只读排查，不动机械臂；查法与判据都写在 HANDOVER 里。

约束：
- 只读优先；任何真机运动前先查 HANDOVER 的「当前严格禁止」
- 结论必须有数值证据：失败写 experiments/，原始 CSV/JSON 写 outputs/，
  只有独立验证通过的稳定结论才进 docs/
- 改完跑 python3 scripts/check_current_docs.py
- 不要开宽泛网络调研、不要开子代理；优先在本地找答案（仓库和 SDK 源码里通常有）
- 读大文件用 offset/limit/grep，不要整读（spill 不覆盖 read 工具）
```

## 为什么这么写

| 设计 | 原因 |
|---|---|
| 只有约 15 行 | 提示词会永久驻留在新会话上下文里，并被之后每一轮重复计费 |
| 先给三个文件路径 | 让新会话**按需定向读**，而不是把内容塞进提示词 |
| 明确"不要假设旧文档已完成" | 这个仓库的历史里"看起来完成了其实失效"是最大坑 |
| 明确禁止宽泛调研/子代理 | 2026-09-18 的成本事故就是 5 个开放式调研子代理造成的 |
| 直接点名 P0-A | 避免新会话从头盘点、重复劳动 |
