# 2026-09-15 候选补偿在线只读试验未执行

尝试以 `config/ft_gravity_calibration.fullrange-robust-20260915.yaml` 将候选补偿
发布到独立实验话题 `/ft_sensor/wrench_compensated_candidate`，不包含机器人运动、
夹爪命令、`tool0` 变换或力控。

`record_ft_static_baseline.py` 在读取 UR 30003 TCP 位姿时失败：
`OSError: [Errno 113] No route to host`。补偿器日志也记录了连续的 TCP 读取失败/超时。
因此没有输出在线残差 JSON，不能把本次尝试作为在线验证证据。

首次试验进程已停止。随后 `scripts/ft_gravity_compensator.py` 已增加
`--max-pose-age`（默认 0.5 s）：TCP 姿态不可用或超过门限时不发布补偿 wrench，
避免 UR 断开后沿用过期姿态。代码已通过 `py_compile` 与 `--help` 检查；仍需在
UR 30003 恢复可达后进行端到端只读验收。

## 同日重试

UR 恢复后重试成功记录到 944 条实验话题消息，但得到均值
`F=[-0.515154, 0.316319, 3.406533] N`、`T=[0.026464, 0.163838, -0.095168] N m`。
该新无接触静态姿态不通过候选的 RMS 门限，详细数据保存在
`outputs/ft_calibration/fullrange-candidate-online-static-20260915.json`；候选已撤销。
日志还显示姿态过期保护反复跳过约 0.5 s 未更新的 TCP，而非沿用过期姿态。
