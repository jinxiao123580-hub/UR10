# 2026-09-16 · 示教轨迹只读记录与离线检查

## 数据

- CSV：`outputs/ur_trajectory/teach-20260916-110054.csv`
- JSON：`outputs/ur_trajectory/teach-20260916-110054.json`
- 来源：UR 30003 实际关节角、TCP 位姿和 TCP 速度；未发送任何控制命令。

## 数值检查

| 指标 | 结果 |
|---|---:|
| 记录时长 | 89.831 s |
| 帧数 | 11170 |
| 平均频率 | 124.345 Hz |
| 帧间隔中位数 / P99 / 最大 | 7.998 / 8.121 / 8.834 ms |
| 重连次数 | 0（初始连接共 1 次） |
| NaN/Inf | 0 |
| TCP 线速度 P99 / 最大 | 0.00529 / 0.00978 m/s |
| TCP 角速度 P99 / 最大 | 0.2065 / 0.2157 rad/s |

起终点 TCP 平移差仅 `0.0496 mm`，但真实旋转差为 `8.860°`；六关节起终点差为
`[-1.190, -1.034, 0.456, -3.518, -7.337, -3.396]°`。因此轨迹只返回了近似相同
空间位置，没有返回相同末端姿态或关节姿态。

按 TCP 线速度 `≤0.001 m/s`、角速度 `≤0.01 rad/s`、持续 `≥3 s` 检测静止段，
全程仅末尾 `86.69..89.82 s` 一段（3.14 s）。这条轨迹当前没有可用于 ATI 静态
重力采样的中间停靠节点。

## 结论

原始轨迹记录完整，可供离线分析；但当前不能直接作为“逐点静态采集并原路返回”的
程序依据。示教器程序需在选定节点插入至少约 14 s 的停留，并将终点修正为记录起点
的完整关节姿态，之后重新记录和验收。
