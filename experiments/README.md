# 试错本

本目录只保存未通过验收的实验、失败现象、原始证据、假设和待验证项。

- 这里的代码和参数不得直接用于教学演示或生产运行。
- 通过独立验收后，只把稳定结论整理到 `docs/`，不把调试过程整段复制过去。
- `outputs/` 保存 CSV/JSON 原始证据；试错文档必须链接对应数据。
- Git 提交分为 `docs:`、`experiment:`、`fix:`、`feat:` 等单一目的，不把教材和失败记录混在同一提交。
