# 2026-09-16 · 示教播放静态组拟合失败

## 输入与排除

输入为 `outputs/ft_repeats/pendant-playback-20260916-final.json` 的 12 组播放采集。
操作者报告第 11 组仍在运动，已由 JSON 的 `operator_excluded_groups` 排除；拟合只用
第 `1..10, 12` 共 11 组。每组使用 5 个通过窗口的均值，原始 CSV 保留在同目录。

## 离线诊断结果

使用 `scripts/calibrate_ft_gravity.py:fit_gravity()`，不连接硬件、不生成补偿话题：

| 指标 | 结果 | 门禁 | 结论 |
|---|---:|---:|---|
| 有效姿态数 | 11 | ≥8 | 满足 |
| 设计矩阵条件数 | 8.207 | ≤120 | 满足 |
| 力 RMS | 4.208 N | ≤1.000 N | **失败** |
| 最大力残差 | 15.411 N | — | 异常大 |
| 力矩 RMS | 0.09005 N·m | ≤0.10000 N·m | 勉强满足 |
| 拟合质量 | 3.2257 kg | 仅诊断值 | 不可作为独立测量 |

## 结论

姿态覆盖不是主要阻塞项；同一重力模型无法解释这些组间的力变化。可能来源包括线缆
预载/走线、移动后未充分静置或其他外部机械状态。不得据此生成有效补偿配置、恢复
补偿进程，或用于力控/碰撞/抓取判断。

下一轮应在每个节点移动后延长静置时间，并记录线缆走线状态；至少保留 3 个未参与
拟合的新姿态做独立验收。
