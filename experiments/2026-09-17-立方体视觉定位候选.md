# 2026-09-17 立方体视觉定位候选

用户在固定棋盘格旁放置标称 `50×50×50 mm` 立方体。使用
`scripts/locate_cube_near_checkerboard.py` 同步采集二维图、普通点云和 UR 30003
实际位姿；未发送机器人命令。采集期间机器人变化 `0.061 mm / 0.009°`。

在棋盘格坐标系中搜索高出参考平面 `20–80 mm` 的组织点云连通域，得到 3 个候选。
主候选有 35092 个像素，表现为立方体竖直侧面：

- 侧面位置约 `x=52.76 mm`；
- 可见横向 P5–P95 跨度 `44.28 mm`；
- 该侧面法向厚度仅 `0.295 mm`，说明它不是完整三维包围盒；
- 可见高度受遮挡和阈值影响，中位约 `32.43 mm`。

棋盘格外缘约位于同轴 `54 mm`，结合用户给出的 50 mm 边长，向远离棋盘格方向偏移
25 mm，并从桌面向物块方向偏移 25 mm，推断：

- 棋盘格系物块中心：`[0.07776, -0.11171, -0.02500] m`；
- 相机系物块中心：`[-0.04095, 0.03715, 0.29859] m`；
- base 系物块中心：`[0.63025, -0.09923, 0.01388] m`。

完整候选在 `outputs/vision/cube-candidate-20260917.json`。该中心包含“物块与可见侧面
的哪一侧相接”的几何推断，不是完整顶面直接测量结果，状态保持
`candidate_not_motion_authorized`。下一步只能先验证高于物块的安全悬停点，不得直接
下降或闭合夹爪。
