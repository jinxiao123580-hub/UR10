# 2026-09-14 工作汇总

本文是当天实验和工程变更索引，不是可直接用于教学或力控的结论。

## 已验证

- UR 控制器保护停原因已从飞行记录确认：`C153A0`，Base 关节位置偏离路径。
  当次记录实际负载为 `3.5 kg / [0,0,0.1] m`，不是流式 IK 脚本预期的
  `8.0 kg / [-0.03,0.03,-0.06] m`。
- Dashboard 只读安全监视器静止真机约 `20 Hz` 采样通过；不能替代 30003
  的 125 Hz 运动状态。
- 30003 在线负载回读连续 25 帧稳定为 `3.5 kg / [0,0,0.1] m`。
- Mech-Eye 彩色图和纹理点云均为 `bgr8`/RGB 容器中的三通道相同值，当前
  实际是单色输出；普通点云 1310720 点中约 57% 为有限 XYZ 点。
- 当前静态 ATI 基线已保存：5 秒、1120 点，TCP 位移约 `0.02 mm`，
  原始均值 `F=[4.159,7.382,58.596] N`、
  `T=[2.169,2.160,0.121] Nm`。

## 已采取的安全措施

- IK 真机执行继续禁用，禁止反向返回试验。
- 未通过独立姿态验收的旧重力补偿模型已标记 `valid: false`，补偿进程已停止；
  原始 ATI 数据仍可采集。
- 未使用示教器负载向导测量结果；CB3 当前界面没有 `Measure`，3.5 kg 只是
  控制器现有配置，不是独立测量结论。

## 尚未完成

- 重新采集 8~12 组手动多姿态 ATI 原始数据并独立验收重力模型。
- 在同一 TCP 姿态下跨重启采集第二个静态基线，才能判断开机零漂。
- 用已知尺寸物体验证点云尺度和曲面形状。
- 确定真实工具质量/重心，之后再更新 `set_payload`。

## 证据和工具

- 保护停证据：`outputs/ik/protective-stop-20260914-162031.txt`
- 相机验收：`outputs/camera/capture-validation-20260914-154850.json`
- ATI 基线：`outputs/ft_baseline/baseline-20260914-162630.json`
- 负载只读回读：`scripts/verify_ur_payload.py`
- ATI 静态基线：`scripts/record_ft_static_baseline.py`
- 相机采集验收：`scripts/validate_mecheye_capture.py`
