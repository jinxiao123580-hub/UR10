# 13 · 同步数字孪生

数字孪生是只读的 RViz 运动学显示，不反向控制机器人。当前版本在重构为与官方 ROS 2 Driver
共用状态链之前，不作为课程或真机流程的操作入口。

可复用的离线模型、原始配置和历史验证记录保留在 `ros2_ws/src/ur10_digital_twin/` 与
`experiments/`；恢复使用前必须在官方驱动环境完成独立启动、关节数值一致性与 frame 隔离验收，
并将通过的启动命令和结果更新到本文。
