# 待办与已知问题

本文件只记录**尚未解决或尚未真机验收**的事项。设备参数、协议、标定数据
和已验证结论放在对应子系统文档，不在这里重复维护。完成一项后，把最终
结论写回对应情报文档，并从本文件删除或标记完成。

## P0：视觉物体中心定位独立验收

状态：手眼标定、图像—点云逐角点一致性和人工修正后的真机抓取已通过；但首次由
单个可见侧面加 50 mm 尺寸推断的中心与人工确认中心 XY 相差 `35.84 mm`。当前只能
生成视觉候选，不能自动下降抓取。

- 组合顶面与侧面，或拟合带尺寸先验的三维立方体模型。
- 在物块多个重新摆放位置报告中心误差，而非只报告点云拟合残差。
- 定位通过门限前保留人工确认，不得把本次成功写成全自动视觉抓取完成。

## P0：末端速度控制与非伪逆 IK

状态：`servoj` 单关节 125 Hz 已验收；`speedl` 沿 base X 的
`+5/-5 mm` 低速双向真机验收已通过。末端速度使用
控制器原生 `speedl`，不在电脑端做雅可比伪逆；离散目标位姿使用 Pinocchio
正运动学与 SciPy 有界信赖域最小二乘 IK。

- 继续验收 Y/Z 单轴、角速度、多轴合成和长时运行。
- IK 已通过 100 组可达目标、不可达目标和限位边界离线验收；
  中位求解 2.073 ms，最慢 5.750 ms。
- 将已有的连续解、限位余量、FK 回代误差和工作空间门禁接入
  真机目标下发。首次 2 mm 目标实测只移动 1.458 mm，精度未通过；
  控制器飞行记录确认 `C153A0`（Base 关节路径偏差）保护停。独立 Dashboard
  监视器已在静止真机以 `19.96 Hz` 验收，但尚未接入运动脚本；`--execute`
  继续禁用，不做反向返回运动。
- 验收前不接入视觉抓取、恒力控制或毛笔轨迹。

## P0：重力补偿真机验收

状态：旧全局模型在 2026-09-11 验收不通过，仍为 `valid: false`。2026-09-15
的全范围鲁棒候选曾在 4 个独立姿态上通过力/力矩 RMS `0.374993 N / 0.049220 N m`，
但新增在线静态姿态反例为 `2.000 N / 0.110 N m`，候选已撤销并标记 `valid: false`。
证据见 `experiments/2026-09-15-全范围鲁棒候选独立验收.md`。补偿进程继续停止。

- 确认只有一个 `ati_netft_node.py`，避免抢占 RDT 单播。
- 原始 `/ft_sensor/wrench_raw` 和 `/ft_sensor/wrench` 可继续验证；
  不得仅因候选通过静态验收就启动 `/ft_sensor/wrench_compensated`，更不能把
  “话题存在”作为补偿、坐标变换或力控有效证据。
- 在未参与拟合的至少 3 个静态姿态记录补偿后六维残差。
- 网页只显示原始力；在独立验收通过前，不恢复补偿数据显示或补偿进程。
- 独立姿态验证通过前，不得将补偿值用于碰撞停止或力控。

验收结果回写 `docs/12-力传感器重力补偿.md`。

## P0：ATI 本体、置零与模型约定复核

状态：ATI 曾因错误写入 Bias 导致读数翻倍并短暂不可达；后续已有恢复和
标定记录，但本轮必须重新用设备响应和 ROS 数据确认，不能只引用旧日志。

- 确认 `192.168.1.2` HTTP 与 UDP RDT 当前可达；读取并保存硬件 Bias，
  预期六路全零。
- 确认原始话题连续发布且没有第二个 RDT 消费者。
- 用已知正确的 `base→tool0` TF 对比 `tcp_pose` 轴角转换矩阵，核对
  `R_base_tool`/转置方向及重力向量正负号。
- 对比自由质量拟合和固定质量假设。历史 8 组自由拟合约 `3.24 kg`、力
  RMS `3.55 N`，固定 `8.0 kg` 更差；不得简单归因于姿态覆盖不足。
- 明确 ATI 感测面以下真实质量与 UR `set_payload` 总质量的边界，两者不必
  相等。

验收结果回写 `docs/08-六维力与3D相机.md` 和
`docs/12-力传感器重力补偿.md`。

## P1：写字工具 TCP 标定

状态：`scripts/tcp_calib.py` 合成测试通过，但现场尚未采集；笔尖相对
`tool0` 的平移未知。

2026-09-18 新增 `scripts/calibrate_fingertip_tcp.py`：用于没有笔时以闭合状态的
左夹爪指尖替代笔尖，采用固定尖点多姿态回转法。它只读取 UR 30003，逐样本静止门禁
并落盘，最后 3 个有效姿态独立验收；不会写入控制器 TCP 或发送任何运动/夹爪命令。

- 用固定尖点采集多姿态并拟合笔尖 TCP，记录残差和姿态覆盖。
- 标定后验证同一空间点在不同姿态下的笔尖位置一致。
- 原抓放点位基于 TCP=`tool0`，启用新 TCP 前必须迁移或重新验证点位，
  防止整体偏移。

验收结果回写 `docs/11-机器人自身标定.md`。

## P1：力/力矩到 tool0 坐标系

状态：ATI 输出仍以 `ft_sensor` 为 frame，传感器到 `tool0` 的变换尚未做
独立验证。

- 核对多姿态标定得到的 `rotation_sensor_from_tool` 与机械安装方向。
- 将补偿后 wrench 正确旋转到 `tool0`；若坐标原点不同，力矩还必须包含
  平移引起的 `r × F` 项，不能只旋转六个分量。
- 在多个无接触姿态验证重力方向在基坐标系中始终竖直。

验收结果回写 `docs/12-力传感器重力补偿.md`。

## P1：set_payload 生效验证

状态：仍未完成。2026-09-14 的保护停飞行记录提供了可靠回读：当次 IK 流式
脚本运行期间，全部 3750 帧均为 `3.5 kg / [0,0,0.1] m`，不是预期的
`8.0 kg / [-0.03,0.03,-0.06] m`。因此该脚本中的 8 kg 设置明确没有生效；
这不等于 8 kg 和该重心本身正确。30003 在线回读工具已完成并以连续 25 帧
验收；不同值写入和自动恢复分支未验收。

- 先用控制器负载向导独立测定真实质量和重心，不沿用估计值。
- 修改每个流式控制脚本，在运动程序内部显式设置经验证的负载参数。
- 每次关键运动后从飞行记录 `Payload mass/CoG` 字段确认实际值；“URScript
  无报错”只能证明语法被接受。

验收结果回写 `docs/11-机器人自身标定.md`。

## P1：Mech-Eye 点云端到端验证

状态：2026-09-14 已再次触发普通点云和纹理点云；普通点云 1310720 点中
747804 点有限（57.0529%），FLOAT32 XYZ 字段、point step 和米制数值范围
均合理。纹理点云 RGB 与普通 2D 图一样为三通道完全相同的单色数据。
尚未用已知尺寸物体验证几何尺度和曲面形状。

- 使用已知平面和有明显高度差的曲面/物体检查米制尺度、轴方向和几何形状。
- 在 RViz 或离线数值切片中验证，不以“话题有消息”作为完成标准。

验收结果回写 `docs/09-MechEye相机接入清单.md`。

## P1：运行时健壮性

- UR 30001 暂时断开时，补偿节点不得长期使用过期姿态。
- 网页桥 ROS spin 线程退出时，进程不得继续占用 8080/9090 伪装在线。
- 为补偿计算增加纯函数单元测试，覆盖已知姿态和错误标定文件。
