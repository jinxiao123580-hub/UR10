# 待办与已知问题

本文件只记录**尚未解决或尚未真机验收**的事项。设备参数、协议、标定数据
和已验证结论放在对应子系统文档，不在这里重复维护。完成一项后，把最终
结论写回对应情报文档，并从本文件删除或标记完成。

## P0：重力补偿真机验收

状态：代码已实现并提交，**尚未启动及真机验收**。

- 确认只有一个 `ati_netft_node.py`，避免抢占 RDT 单播。
- 验证 `/ft_sensor/wrench` 与 `/ft_sensor/wrench_compensated` 均连续发布，
  补偿话题频率接近原始 200 Hz。
- 在未参与拟合的至少 3 个静态姿态记录补偿后六维残差。
- 网页切换“原始/重力补偿”，确认数值、状态文字和 10 秒折线同步切换。
- 独立姿态验证通过前，不得将补偿值用于碰撞停止或力控。

```bash
ros2 topic hz /ft_sensor/wrench
ros2 topic hz /ft_sensor/wrench_compensated
ros2 topic echo /ft_sensor/wrench_compensated --once
```

验收结果回写 `docs/12-力传感器重力补偿.md`。

## P0：网页相机彩色画面呈灰色

状态：真实画面已到达网页，分辨率验证为 `1280x1024 JPEG`，但用户现场
观察为灰色，**根因未确认**。

1. 记录 `/mechmind/color_image` 的 `encoding`、`step`、宽高和数据长度。
2. 检查 `scripts/ros_web_bridge.py::_image_to_jpeg()` 的实际编码转换。
3. 检查 Bayer、YUV、RGBA 及实际为 `mono8` 的情况，不能仅凭话题名称
   假定数据为 `bgr8/rgb8`。

```bash
ros2 topic echo /mechmind/color_image --once --field encoding
ros2 topic echo /mechmind/color_image --once --field step
ros2 topic info /mechmind/color_image -v
```

验收标准：

- 网页与 Mech-Eye Viewer 的同一场景颜色一致，红蓝不互换。
- 对含红、绿、蓝物体的画面验证存在 RGB 通道值不相等的像素。
- WebSocket 图像为 JPEG，尺寸与 ROS 彩色图一致。
- 连续采集无服务积压，深度伪彩色处理不影响彩色话题。

验收结果回写 `docs/10-网页监控台.md` 和
`docs/09-MechEye相机接入清单.md`。

## P1：运行时健壮性

- UR 30001 暂时断开时，补偿节点不得长期使用过期姿态。
- 网页桥 ROS spin 线程退出时，进程不得继续占用 8080/9090 伪装在线。
- 为补偿计算增加纯函数单元测试，覆盖已知姿态和错误标定文件。
